## 执行模式选择

STDD 支持两种执行模式：

### 普通交互模式（默认）
每个 Phase 产出后等待用户确认，适合谨慎推进的变更。

### 长程自动模式
在 Gate 2 确认后，Phase 3-5 连续自动执行：
- 自动切片规划 → TDD 实现 → 质量验证
- 设计偏差自动记录
- 达到迭代上限时暂停并汇总

**切换方式**：
- 启用长程：Gate 2 确认时选择"全自动长程模式"
- 退出长程：在长程执行期间输入"切换普通模式"
