# 安全基线 / Security Baseline

> 始终在线规则

## 关键检查项

1. **注入防护**：SQL 使用参数化查询，OS 命令使用参数列表而非字符串拼接
2. **认证授权**：每个端点验证 Token，权限检查不可跳过
3. **敏感数据**：不在日志中打印密码/Token/密钥
4. **依赖安全**：不引入已知 CVE 的依赖版本
5. **配置安全**：生产环境 DEBUG=False，CORS 白名单，默认密钥必须修改
